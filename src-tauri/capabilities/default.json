{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the OpenShores launcher window",
  "windows": ["main", "theme-editor"],
  "permissions": ["core:default", "os:default"]
}
